# 字体宽度数据文件，如果不存在，将读取内置的字体宽度数据
fonts-file: 'font.json'
# 悬浮字字体宽度与世界单位尺寸（一个方块的边长）的比例缩放关系
font-char-scale:
  # 样例字符
  sample-char: ' '
  # 多少个字符代表一个完整方块的边长 9+(20/27)
  location-scale: 9.7407407407407407
